simpleweb
=========

A simple web framework in Elixir

## Running the server ##

First time:

```
make
./start.sh
```

Subsequent times:

```
# Will only rebuild Elixir source and run the server
make run
```
